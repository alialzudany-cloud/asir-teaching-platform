services:
  asir-teaching-platform:
    build: .
    ports:
      - "8020:8020"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    restart: unless-stopped
